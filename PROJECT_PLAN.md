# 旅游攻略助手 (Travel Guide Assistant) - 深度开发计划书

## 项目概述
本项目旨在基于 **HarmonyOS Next (API 11+)** 和 **DevEco Studio** 开发一款旗舰级旅游攻略助手应用。项目将严格遵循鸿蒙设计规范（HarmonyOS Design），采用声明式 ArkTS 开发，深度集成分布式能力、元服务及 AI 智慧服务，打造“大气便捷、视觉华丽、交互流畅”的下一代旅游应用。

## 一、 基础架构与技术栈 (Infrastructure)
### 1.1 开发环境
- **IDE**: DevEco Studio Next Release
- **SDK**: HarmonyOS Next API 11+
- **Language**: ArkTS (Stage 模型)
- **Build System**: Hvigor

### 1.2 架构设计
- **MVVM 模式**: View (UI) - ViewModel (状态管理) - Model (数据层) 分离。
- **模块化**: 
  - `entry`: 主入口，包含核心页面与逻辑。
  - `commons`: 公共组件、工具类、资源文件。
  - `features`: 独立业务模块（如地图服务、AI 助手）。

#### 系统架构图 (System Architecture)
```mermaid
graph TD
    A[UI Layer (ArkUI)] --> B[ViewModel Layer (Observed/State)]
    B --> C[Model Layer (Data Repository)]
    C --> D1[Local Data (RDB/Preferences)]
    C --> D2[Remote Data (Network/API)]
    
    subgraph "Feature Modules"
        F1[MapService]
        F2[AIService]
        F3[ImageLoader]
        F4[TripManager]
    end
    
    B --> F1
    B --> F2
    B --> F3
    B --> F4
```

### 1.3 代码规范与封装原则
所有代码和功能的实现必须遵循高内聚、低耦合的封装原则：
1. **模块化设计**: 独立封装，明确接口，隐藏实现。
2. **代码结构**: 清晰目录，遵循命名规范。
3. **接口设计**: 简洁明确，严格类型，完整注释。
4. **可维护性**: 关键注释，逻辑分离，配置化。
5. **扩展性**: 预留扩展点，支持插件化。
6. **质量保证**: 单元测试，错误处理。
7. **文档要求**: 功能说明，API手册。

### 1.4 资源与主题
- **设计系统**: 建立全局 Token 系统（颜色、排版、间距、圆角）。
- **图标库**: 本地高精 SVG 图标库，支持动态着色。
- **深色模式**: 全局适配深色/浅色主题切换。

## 二、 核心 UI 布局与交互 (UI & UX Strict Requirements)

### 2.1 首页 (Home Page)
*布局容器: `Tabs` (BottomNavigation)*
#### A. 顶部导航与状态栏
- **沉浸式设计**: 内容延伸至状态栏。
- **双 Tab 导航**: “首页”与“我的”。

#### B. 头部核心区 (Header Area)
1.  **必玩推荐轮播 (Hero Swiper)**:
    - 卡片式设计，16:9，自动轮播。
    - 内容: 高清景点大图 + 渐变蒙层 + 醒目大标题。
2.  **功能悬浮球**:
    - **搜索按钮**: 右上角悬浮。
    - **登录/注册**: 左上角。

#### C. 核心功能四宫格 (Functional Grid)
- **模块定义**:
  1.  **新建行程**: 蓝紫渐变，图标：地图/加号。
  2.  **旅游笔记**: 橙红渐变，图标：记事本/笔。
  3.  **旅游建议**: 青蓝渐变，图标：指南针/AI 机器人。
  4.  **最近行程**: 红粉渐变，图标：足迹/时间轴。

#### D. 推荐列表 (Feed List)
- **图文卡片**: 大图展示 + 标题 + 作者头像 + 点赞数。
- **交互**: 点击跳转详情页，支持转场动画。

### 2.2 搜索页面 (Search Page)
- **自动聚焦**: 弹出软键盘。
- **历史记录**: 标签展示。
- **实时联想**: API 联想地点。
- **图片展示**: 搜索结果包含精美缩略图。

### 2.3 个人中心页面 (Mine Page)
- **头部**: 用户信息。
- **数据栏**: 社交数据。
- **功能列表**: 收藏、历史、设置。

## 三、 功能逻辑深度实现 (Functional Requirements)

### 3.1 新建行程 (Create Trip) - 已实现核心
- **流程**: 输入行程名 -> 地名检索 -> 地图规划 -> 添加地点。
- **地名检索**: 集成 Map Kit，支持模糊匹配。
- **行程地图**: 标记起点/终点，绘制路线 (Polyline)。
- **智能规划**: AI 辅助生成最优路线。

### 3.2 旅游笔记 (Travel Notes) - 待完善
1.  **富文本编辑器 (Rich Editor)**:
    - **技术方案**: 使用 ArkUI `RichEditor` 组件。
    - **功能**: 支持文本样式（加粗、斜体）、图片插入（相册/拍照）。
    - **实现**: 封装 `NoteEditor` 组件，提供工具栏操作。
2.  **关联行程**:
    - 笔记数据模型中增加 `tripId` 字段，关联具体行程。
3.  **打卡模式**:
    - **手动打卡**: 搜索 -> 详情页 -> 点击打卡。
    - **自动打卡**: 基于 Geo-fencing (地理围栏)，进入范围自动触发。
    - **分享卡片**: 使用 `Canvas` 绘制打卡海报（地图截图 + 照片 + 文字），支持保存到相册。

### 3.3 旅游建议 (Travel Advice) - AI 赋能
1.  **AI 问答**:
    - **技术方案**: 集成 HTTP 客户端调用大模型 API (如文心一言/ChatGPT/华为盘古)。
    - **界面**: 仿聊天 IM 界面，支持流式输出 (Server-Sent Events)。
2.  **实时工具**:
    - **汇率/天气**: 调用第三方开放 API (如和风天气)。

### 3.4 最近行程 (Recent Trips)
1.  **数据存储**:
    - 使用 `RelationalStore` 存储行程表 (`Trip`) 和地点表 (`Place`)。
2.  **UI 实现**:
    - **时间轴**: 使用 `List` + `Canvas` 绘制连接线。
    - **状态管理**: 根据当前时间对比行程时间，自动计算状态（未开始/进行中/已结束）。

### 3.5 景点详情卡片页
- **结构**: `Scroll` 容器。
- **内容**: 大图 Banner -> 信息卡片 (Info Card) -> 介绍文本 (Text) -> 底部操作栏 (ToolBar)。
- **交互**: 
  - "添加到行程": 触发 `AppStorage` 更新或回调。
  - "打卡": 记录打卡数据至本地数据库。

### 3.6 图片资源管理模块 (Image Management) - **新增核心**
本模块负责应用内所有景点图片的获取、缓存和展示，解决无图或加载慢的问题。

#### A. 技术方案
1.  **图片来源**:
    - **主源**: **Pixabay API** (免费商用/无版权)。
      - 机制: 使用 `http` 模块调用 Pixabay Search API，关键词为景点名称 (英文优先，中文需翻译)。
      - *注: 需申请 Pixabay API Key，或使用模拟 Mock 数据。*
    - **备选源**: **高德地图/百度地图 API** (POI 图片)。
      - 机制: 通过 POI ID 获取详情，提取图片 URL。
      - *注: 需遵循地图服务商的使用条款，通常要求在地图容器内展示。*
2.  **下载与缓存 (ImageLoader)**:
    - **内存缓存**: 使用 LRU 策略缓存已加载的 `PixelMap`。
    - **磁盘缓存**: 将下载的图片文件保存至应用沙箱 `cacheDir`。
    - **加载流程**: 内存 -> 磁盘 -> 网络。

#### B. 数据模型与规范
- **存储路径**: `/data/storage/el2/base/haps/entry/cache/images/`
- **命名规范**: `img_{place_id}_{timestamp}.jpg`
- **版权合规**: 
  - 优先使用 CC0 协议图片。
  - 界面上预留 "Image Source: Pixabay" 微小标注以示尊重。

#### C. 异常处理
- **网络异常**: 加载失败显示默认占位图 (`ic_placeholder.svg`)。
- **无结果**: 若搜索不到图片，使用随机风景图作为兜底。

## 四、 鸿蒙原生能力接入
### 4.1 分布式流转
- **场景**: 手机规划 -> 平板查看。
- **技术**: `DistributedDataObject` 同步行程状态。

### 4.2 元服务
- **服务卡片**: 2x2 (倒计时), 2x4 (行程概览)。
- **实现**: `FormExtensionAbility`。

### 4.3 创新交互
- **手势**: 图片双指缩放，列表侧滑删除。
- **动效**: 共享元素转场 (`geometryTransition`)。

## 五、 数据与存储
- **用户偏好**: `Preferences` 存储设置。
- **业务数据**: `RelationalStore` (SQLite) 存储核心数据。
- **事务保护**: 数据库写入操作必须包裹在事务中，确保一致性。

## 六、 异常处理与质量保障
### 6.1 全局异常捕获
- 配置 `AbilityLifecycleCallback` 监控崩溃。
- 网络请求封装层统一处理 404/500 错误，返回标准化 `Result<T>` 对象。

### 6.2 性能标准
- **列表**: 使用 `LazyForEach` 渲染长列表，确保帧率 > 55fps。
- **图片**: 列表页加载缩略图 (previewURL)，详情页加载大图 (largeImageURL)。

## 七、 项目开发里程碑 (Milestones)

### 第一阶段：核心功能搭建 (Week 1-2)
- [x] 项目初始化与架构搭建
- [x] 新建行程页面 UI 与逻辑 (NewTripPage)
- [x] 搜索页面与数据传递 (SearchPage)
- [ ] 数据库设计与 CRUD 封装 (Trip/Place Model)

### 第二阶段：资源与扩展功能 (Week 3)
- [ ] **图片管理模块**: 实现 Pixabay API 对接与图片缓存。
- [ ] **旅游笔记**: 富文本编辑器开发。
- [ ] **详情页**: 景点详情与打卡功能。

### 第三阶段：智能化与优化 (Week 4)
- [ ] **AI 建议**: 对接 AI 接口实现问答。
- [ ] **地图深度集成**: 真实导航路线绘制。
- [ ] **性能优化**: 列表懒加载、图片预加载。

### 第四阶段：测试与交付 (Week 5)
- [ ] 单元测试 (JUnit/ArkUnit)。
- [ ] 多设备适配测试 (Phone/Tablet)。
- [ ] UI 验收与 Bug 修复。

## 八、 测试方案与验收标准
1.  **功能测试**: 
    - 搜索结果准确，图片加载正常（无闪烁、无错位）。
    - 行程增删改查数据持久化无误。
2.  **性能测试**:
    - 连续滑动列表 1 分钟，无明显卡顿。
    - 弱网环境下，图片加载具备重试机制或占位图。
3.  **兼容性测试**:
    - 在折叠屏展开/折叠状态下，布局自动响应。
    - 深色模式下，文字与图标对比度符合 WCAG 标准。
